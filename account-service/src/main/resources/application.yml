eureka:
    instance:
        preferIpAddress: true
        ipAddress: 3.35.50.79
    client:
        registerWithEureka: true
        fetchRegistry: true
        serviceUrl:
            # defaultZone: http://localhost:8761/eureka/
            defaultZone: http://3.36.116.95:8761/eureka/
management:
    endpoint:
        health:
            show-details: always
            diskspace:
                enabled: true
        env:
            show-details: always
            enabled: true
